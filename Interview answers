/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>
#include <iostream>
#include <ctype.h>
#include <stdio.h>
#include <algorithm>
#include <cctype>
#include <string>

using namespace std;

bool isPalindrome(string str) 
{
int length = str.length(); 
for (int i = 0; i < length / 2;i++) {
    
  char c;
  if (str[i] != str[length - 1 - i]) {
         return false;
    }     
  } 
 return true;
}

    int main() {
      string Word;
  cout << "Enter object for analysis: " << endl;
  cin >> Word;
  if(isPalindrome(Word))
  cout << Word <<" is a palindrome.";
    else cout << Word <<" is not a palindrome.";
      return 0;
}

/////////////////////////////////////////////////////////////////////////////////

2)select * from(select ename, sal, dense_rank() 
over(order by sal desc)r from Employee) where r=&5;f

3)SELECT emp_country, COUNT(*)
  FROM emp_details
  GROUP BY emp_country;
